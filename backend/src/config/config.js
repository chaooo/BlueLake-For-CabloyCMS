// eslint-disable-next-line
module.exports = appInfo => {
  const config = {};

  // theme
  config.theme = {
    _theme: {
      name: 'BlueLake',
      url: 'https://github.com/chaooo/BlueLake-For-CabloyCMS',
    },
  };

  return config;
};
